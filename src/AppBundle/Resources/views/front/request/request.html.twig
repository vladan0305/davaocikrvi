{% extends '@App/front/master/layout.html.twig' %}

{% block body %}
    
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    {{form_errors(form)}}
    
     {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    {{form_errors(form)}}
    
     {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    {{form_errors(form)}}

    {{ form_widget(form.submit) }}
    
{% endblock %}