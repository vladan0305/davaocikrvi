{% extends '@App/admin/master/layout.html.twig' %}
{% set page_title = 'Mesta' %}

{% block body %}

    <div class="col-lg-12">
        <h1 class="page-header">
            Modul Mesta

            <a href="{{ path('admin_requestplace_create') }}" class="btn btn-success" style="float: right;">
                Dodaj Novo
            </a>
        </h1>
    </div>

    <div class="col-lg-12">
        <div class="panel panel-default">

            <div class="panel-heading">
                Naziv Tabele
            </div>

            <div class="panel-body">
                <div>
                    <table class="table table-striped table-bordered table-hover">
                        <thead>
                            <tr>
                                <th style="width: 4%">#</th>
                                <th class="text-center">Ime Grada</th>
                                <th class="text-center">Adresa</th>
                                <th class="text-center">Tip</th>
                                <th class="text-center" style="width: 20%">Akcije</th>
                            </tr>
                        </thead>
                        <tbody>

                        {% if requestplaces is not empty %}

                            {% for place in requestplaces %}
                            <tr>

                                <td class="text-center">{{ loop.index }}</td>
                                <td class="text-center">{{ place.city.name }}</td>
                                <td class="text-center">{{ place.address }}</td>
                                <td class="text-center">{{ place.type }}</td>

                                <td class="text-center">
                                    <a href="{{ path('admin_requestplace_edit', {'id': place.getId()}) }}"
                                       class="btn btn-primary">
                                       <i class="fa fa-pencil"></i>
                                    </a>
                                    <a href="{{ path('admin_requestplace_delete', {'id': place.getId()}) }}" class="btn btn-danger">
                                        <i class="fa fa-trash"></i>
                                    </a>
                                </td>

                            </tr>
                            {% endfor %}
                        {% endif %}

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

{% endblock %}